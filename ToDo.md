 TODO List

 ❎   ИСПРАВИТЬ если нажать пи а следом е, то они склеются на экране, а должна произойти замена!! 

 ❎ двойной клик на ячейке памяти: ПЕРВЫЙ КЛИК - вывОДИТ число из памяти на экран, А ВТОРОЙ сохранЯЕт в ячейке

 ❎ почему, когда я делаю цепочку [M1]+[M2]+[M3]+[M4], то  М1 извлекается дважды

 ❎  ERROR → M1 запись ОШИБКИ в ячейку памяти???

  ❎  при большом количестве записей в истории - сползает вниз текущая строка  <p		class="main-display" 	> /
  Как её зафиксировать, чтоб она не двигалась вниз? что перепроверить?

  ❎  не работает скролл в 	.history-section 

 ❎   # запустить скрипт для оптимизации картинок

❎  Cделать компонент picture принимает url для jpeg, url webp, alt, customClass. использовать лучшие практики типа lazyLoad 

❎  исправить, чтоб в истории вместо -- были скобки вокруг числа 
-4--4=0 
-14--4=10

❎  из истории строки вида 777 -> М2 при длительном нажатии сохраняются БЕЗ  -> М2, а только число!

❎  заменить svg для info и остальных кнопок на актуальное из коллекции part44


❎  адаптив layout для basic/
❎  кнопки цифр, ссылок, памяти поправить на мобильном портретном и горизонтальном и
добавить padding снизу, чтоб экран не обрезал кнопки


❎   Добавить полосу снизу для рекламной строки - банера


❌ 5 языков - это ИЗБЫТОЧНО ДЛЯ ЭТОГО ПЕТ-ПРОЕКТА,  правильно для SEO через отдельные ветки routes/en/...all folders и так для каждого языка routes/es/...all folders 

❎ слияние с веткой main
❎ настроить deploy и Action для этого проекта




# BRANCH NOW toFixBugs


 ❎    исправить!
если большое количество строк в истории, то экран налазит на кнопки. 
Началось после добавления строки с банером.

 ❎    исправить!!!
стили на мобильном для функций

 ❎    исправить новую ошибку. Это происходит с любой константой, а не только Пи и е
16.7 = 16.7
+ [e] [=]
16.7 = 16.7
+ [п] [=]
16.7 = 16.7
+ [п] + [e] [=]
16.7 = 16.7


 ❎    basic.js - разгруппировать на engineer and basic

 ❎   исправить 2 задачи 
447=3*149
divs:447=3*149:58 
divs:447=3*149:58 = 
divs:447=3*149:58 - тут должно быть ERROR (задача1) 
149 ∩ 58 : []
3*149:58 → M1 - если есть divs, то сохраняем всю строку (как divs:447=3*149:58), а не только то что за знаком равно (задача2)

448 ∩ 24 : [2, 4, 8]
448 ∩ 24 : [2, 4, 8] → M1 - сохраняется вся строка - это правильно
448=2*2*2*2*2*2*7 - теперь нажимаем кнопку [X*Y] и вводим 55 получаем:
divs:448=2*2*2*2*2*2*7:55 - это недопустимое действие! 
divs:448=2*2*2*2*2*2*7:55 = divs:448=2*2*2*2*2*2*7:55 - тут после нажатия = должно быть ERROR (задача1)
 

 ❎  
447=3*149 
divs:447=3*149:58 

NaN - на дисплее должно быть ERROR вместо NaN

 ❎  есть число на дисплее как результат предыдущего действия, но не 0 и нажимаем √х - то не заменяем число, а извлекаем корень из этого числа

 ❎  Если  нажать √х, то последующее извлечение числа из памяти должно добавить цифру к корню, а сейчас происходит замена символа корня на число из памяти!

 ❎  если из памяти извлекли число, но пользователю нужно ввести другое число и при этом он не использует кнопки backspase и C, а просто начинает вводить новое число, то нажимаемые цифры приклеиваются к извлечённому из памяти, а должны заменять извлечённое число! 
если из памяти извлекли число, но пользовотелю нужно ввести константу (пи, е и т.п.) через кнопку константы, и при этом он не использует кнопки backspase и C, то извлечённое число заменяется константой

 ❎  если на дисплее ERROR, то Backspace - очищает не по символьно, а сразу всё слово



 ❎  Инженерная нотация (EE или Exp): Возможность вводить числа типа 5*10^12. 5 на 10 в 12 степени
 *10^y
 

 ❎  1. Математические константы
\pi (Пи): Math.PI (~3.14159). Основа всей тригонометрии.
e (Число Эйлера): Math.E (~2.71828). Основа натуральных логарифмов и роста.
\phi (Золотое сечение): (1 + Math.sqrt(5)) / 2 (~1.618). Полезно для дизайнеров и архитекторов.
\sqrt{2}: Math.SQRT2 (~1.414). Часто встречается в геометрии и электротехнике.

 ❎  убрать (9) в периоде при
			10*10^24 = 9.999999999999999e+24
			1*10^24 = 1e+24
			5*10^24 = 4.999999999999999e+24
			100*10^24 = 1e+26
			2.5*10^24 = 2.4999999999999997e+24

 ❎  стили для М1...М4 на моб горизонт

 ❎  Подготовить иконки для калькуляторов:
	figma:
		убрать контуры
		масштаб до 512px
	скачать
	обработать:
	  jpeg webp 512 и 64px оптимизировать

 ❎  favicon контрастный и icon-192 icon-512 для sw 

 ❎  Все картинки превратить в picture


 ❎  Каталог оформить как основной в 44path  c накладками текста

❌ из a.catalog__item  для src/routes/(math)/basic/+page.svelte сделать автоматический слайдер из разных режимов
 ❎   каталог, как многоуровневое открытое меню через details? 



 ❎  убрать,		screen and (pointer: coarse) и префиксиры.

 ❎  # карточки основного каталога без надписи, надпись появляется только при :hover
 ❎   убрать красный контур карточки при клике  :active


 ❎    применить для строк из истории при длительном нажатии для сохранения в М1
.catalog__card {
  -webkit-tap-highlight-color: transparent; /* Убирает синий квадрат при нажатии в Safari */
  -webkit-touch-callout: none;              /* Убирает системное меню при долгом нажатии */
  user-select: none;                        /* Предотвращает выделение текста внутри */
  touch-action: manipulation;               /* Оптимизирует задержку клика */
}
 ❎   тест на мобильном: отменить действие по умолчанию для длительного нажатия на строку истории. Сейчас появляется андроид-меню [копировать, вставить] - ДОБАВИТЬ В КОНЕЦ СТРОКИ КНОПКУ [-> M]



# BRANCH navMore

 ❎    для карточек на главной странице все :hover и :active  для мобильных.
 ❎    есть ссылка с картинкой внутри. стили для :hover и :active исправны. Как внести такие изменения: если сайт открыт на мобильном (нет эффекта наведения - устройство без мыши), то после первого клика сначала показываем :hover (за 0.35 сек ) эффект, а после второго отрабатываем эффект :active и уже после этого само действие ссылки или кнопки. Ещё раз есть ссылки-карточки: первый клик на мобильном должен соответствовать :hover эффекту на ПК, а второй клик на мобильном должен соответствовать :active на ПК.

 ❎   исправить
1*10^22 = 1e+22
1e+22*10000 = 1e+26
√1e+26
ERROR


============================================
============================================


# BRANCH navNow

 ❎   для страниц в папке (home) - nav с вложениями  из готовых компонентов


 ❎   разобраться с сылками с учётом перспективы применения i18n (src/routes/[[lang]]/(math)/basic/+page.svelte).
<a href="{base}/history"> или <a href="/history"> При локальном тестировании через npm run serv - работают обе ссылки, после публикации ссылки без {base} - не работают.


Переменная base импортируется из $app/paths. Она подставляет префикс пути, который вы указали в svelte.config.js (поле paths.base). Если вы публикуете на GitHub Pages или в подпапку хостинга, SvelteKit сам добавит нужный префикс, и ссылка станет правильной: /project/history.

Относительный путь через SvelteKit     <a href="{base}/history"> 
Ссылка на этой же странице           	<a href="#id">
Ссылка на главную (к якорю)	         <a href="{base}/#id">
Ссылка внутри другого раздела	     <a href="{base}/history#id">


❌  для страниц в папке (home) -  header sticky 
 
===== installPage



 ❎   appStore.canInstall

 ❎   # проверить доступность установки приложения 
appStore.canInstall = true - можно установить приложение (есть разрешение от браузера)
appStore.canInstall = false - нельзя установить приложение (НЕТ разрешения от браузера)

 ❎   Если appStore.installed = false - не установлено PWA, и appStore.canInstall = true - можно установить приложение, то если возможна установка приложения:

 ❎   внутри nav  - появляется раздел [install]  -> переход на страницу install

 ❎   для страниц в папке (home) - появляется кнопка в header download and install this app -> вызов окна установки или переход на страницу install если пользователь отменил установку ранее

 ❎   для страниц в папке HE (home) и появляется небольшая яркая кнопка [install]  -> переход на страницу install

 ❎   на странице install - появляется большая яркая кнопка [installNow] или пояснение как установить



===========  makeStores & setPage


 ❎   синхронизировать реактивное состояние $state с localStorage с помощью $effect, обеспечивая сохранение данных между сессиями браузера.

 ❎   если ещё нет, то создаём appStore и historyStore/ Если appStore есть, то получаем его из localStorage

 ❎   appStore - хранит данные влияющие на всё приложение, например данные из setPage. 

 ❎   setPage - создать под этот appStore  и протестить запись и хранение  новых значений 

localStorage до 5Мб или 2,5 млн символов.

 ❎   historyStore. Перед выходом из приложения - сохраняем объект с датой и массивом вычислений. Если кнопкой "HistoryOld" вызывается история, то открываем страницу (home)/historyOld и показываем sectionЫ с датой сохранения h2 и списком вычислений. 

 ❎    страница history - обязательно сейчас сделать вывод из localStorage в эту страницу!!!!

 ❎   тестировать historyStore на ПК

 ❎    тестировать historyStore на мобильном:
  ❎   не работает settings количество знакой

============================================
============================================

# BRANCH quickChoise

  ❎   аналогично с #registry  создать Реестр существующих вариантов для каждой группы страниц и select в правом верхнем углу - как быстрый переход к другому калькулятору внутри группы (math/basic math/engin math/trigon или geom/triang geom/circle geom/square и т.п.)  - это отдельные компоненты для каждой группы страниц
ИЛИ
  ❎   Для быстроого перехода внутри группы страниц - кнопки с лого в верхнем правом углу дисплея. Текущая выделена акцентом.   

 ❎  применить ³ - &sup3; надстрочная цифра для корня для моего случая и любой цифры, например 23 или 12

  ❎  поддержка цифр и enter для = на desktope

  ❎  Кнопка вверх. Напиши функцию, которая принимает: id - якорь для элемента страницы и создаёт кнопку "вверх". Кнопку необходимо именно создать через JavaScript и установить стили для неё. Если элемент с якорем не виден на странице - прокрутили страницу, то появляется кнопка вверх. На этой кнопке изображена стрелка вверх. Стили для кнопки: фон coral, стрелка белого цвета и кнопка должна выглядеть футуристичной и объемной.

============================================
============================================

# BRANCH style

  ❎  создать палитру на базе src/lib/components/Btn/BtnText.svelte и 

  ❎  1 Команда для поиска всех цветов в проекте.
в VSCodium, для Svelte-проекта, чтобы не сканировать лишний мусор (картинки, кеш и модули):

Bash
grep -rEoh "#[0-9a-fA-F]{3,6}|rgba?\(.*?\)|hsla?\(.*?\)" --exclude-dir={node_modules,.svelte-kit,.git} . | sort | uniq

Что она сделает:
-r: Ищет рекурсивно во всех папках.
-Eoh: Использует расширенные регулярки, выводит только само совпадение (без лишнего текста) и скрывает имена файлов.
#...|rgba...: Ищет HEX, RGB и HSL цвета.
--exclude-dir: Игнорирует папку библиотек и служебную папку SvelteKit (чтобы не собрать тысячи цветов из чужих пакетов).
| sort | uniq: Сортирует их по алфавиту и удаляет дубликаты.

  ❎  2. заполнить _variables.scss - берём скриншоты из figma и получаем список переменных

  ❎   3. даём _variables.scss и берём стили компонента - ИИ преобразует похожие цвета в переменные - все стили унифицировать с _variables.scss

  ❎  тёмная тема готова

 // 1. Базовые цвета (Neutrals)
 $clr-bg: #1f1f1f;
 $clr-bg-darker: #000d37;
 $clr-bg-card: #0f172a;
 $clr-white: #ffffff;
 $clr-black: #000000;
 $clr-slate: #64748b; // Тот самый недостающий серый (бывший $clr-text-gray)

 // 2. Акценты и Неон (The Glow)
 $clr-mint: #01d9c3;
 $clr-mint-soft: #86edf2;
 $clr-sky: #00d1ff;
 $clr-coral: #ff7f50;

 // Цвета для специфических элементов (синие оттенки из дизайна)
 $clr-blue-light: #02427e;
 $clr-blue-mid: #02273e;
 $clr-blue-hover: #344e66;
 $clr-blue-deep: #00051a;

 // 3. Градиенты
 $grad-btn-main: linear-gradient(215deg, $clr-blue-light 0%, $clr-bg-darker 50%, $clr-blue-mid 100%);
 $grad-op: linear-gradient(45deg, $clr-slate 0%, #252c35 50%, #4b5768 100%);
 $grad-memo: linear-gradient(45deg, $clr-slate 0%, $clr-mint 50%, #4b5768 100%);

 // 4. Тени и Свечение
 $shd-mint-glow: rgba($clr-mint, 0.5);
 $shd-coral-glow: rgba($clr-coral, 0.5);
 $shd-blue-glow: rgba(100, 170, 255, 0.7);

 $shadow-neon-mint: 0 0 8px $shd-mint-glow;
 $shadow-neon-coral: 0 0 8px $shd-coral-glow;
 $shadow-card: 2px 2px 2px 2px #586466;
 $shadow-deep: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
 // Внутреннее неоновое свечение (Mint)
 $shadow-inset: inset 4px 4px 8px rgba($clr-mint, 0.33),
 	inset -4px -4px 8px rgba($clr-mint, 0.33);

  ❎   светлая тема

  ❎   переключение тем 

  ❎  src/lib/components/Display/DisText.svelte и <p use:longpress


  ❎  проверка на безопасное использование с $effect(() => { и onMount(() => {

  
===================================================================
===================================================================
===================================================================




# Branch trigon

  ❎  после последней публикации если открыть не через VSCodium а зайти на сайт:  
X1. не работают QuickMenu которые создавались. Вместо картинок там текст - сработал {else} блок в коде:
{#if currentMap}
		<div class="quick-nav-container">
			<QuickMenu items={currentMap} />
		</div>
	{:else}
		<div class="now_mode">
			<p>{appState.now_mode}</p>
		</div>
	{/if}
X2. остались и не удалились console.log - они не удаляются автоматически при сборке!
X3. Приходится в ручную обновлять страницу, чтоб подтянулись обновлённые стили
X4. не обновились стили некоторых элементов даже после перезагрузки.
X5. поломаны стили для функциональных кнопок на мобильном и не видно рекламы внизу!!
X6. не устанавлмвается на телефон. пишет, что предыдущий сайт ещё не установлен!
X7. в консоли: sw.js:1 Uncaught (in promise) TypeError: Failed to execute 'addAll' on 'Cache': Request failed
 
  ❎  перевести проект на @vite-pwa/sveltekit, чтоб после обновления приложения оно АВТОМАТИЧЕСКИ обновлялось и у пользователей.

старый vite.config.js

import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';
import path from 'path';
import autoprefixer from 'autoprefixer';

export default defineConfig({
	plugins: [sveltekit()],

	resolve: {
		alias: {
			'$utils': path.resolve('./src/lib/components/utils'),
		},
	},

	// ДЛЯ УСКОРЕНИЯ горячей перезагрузки модулей (HMR - Hot Module Replacement), 
	// вызванный проблемами с файловым наблюдателем (File Watcher) 
	// или недостаточной производительностью компиляции.
	server: {
		watch: {
			ignored: ['**/node_modules/**']
		}
	},

	css: {
		// PostCSS для Autoprefixer
		// Пример для более широкой поддержки	для древних браузеров 
		// overrideBrowserslist: ['> 0.5%', 'last 4 versions', 'Firefox ESR']
		postcss: {
			plugins: [
				autoprefixer({
					overrideBrowserslist: ['last 2 versions', 'not dead']
				})],
		},

		// Настройка SCSS препроцессора
		preprocessorOptions: {
			scss: {
				// Глобальные переменные SCSS
				// Этот файл будет автоматически импортирован во все Svelte-компоненты
				// БЕЗ необходимости писать @import ... в каждом файле.
				// НО если ты поместишь _reset.scss или _layout.scss СЮДА, ТО вес твоего CSS вырастет в десятки раз.
				// ДЛЯ _reset.scss и _layout.scss ИМПОРТИРУЙ В src/styles/app.scss, КОТОРЫЙ ИМПОРТИРУЕТСЯ В src/routes/+layout.svelte
				additionalData: `
    @use "${path.resolve('src/styles/variables').replace(/\\/g, '/')}" as *;
    @use "${path.resolve('src/styles/mixins').replace(/\\/g, '/')}" as *;
  `
			}
		},

		// 3. Настройка CSS Modules (опционально)
		modules: {
			localsConvention: 'camelCase',
			generateScopedName: '[name]__[local]--[hash:base64:5]',
		},
	},
});




  
===================================================================
===================================================================
===================================================================




# Branch trigonoMetr
# Branch trig





# Если PWA установлен, то вместо кнопки установить - поставить поделиться.











# тестировать Memory и bind связку (но inputы нельзя, чтоб не появлялась экранная клавиатура) и отдельный stor для геометрии треугольник. Для каждой фигуры свой stor.


#  тригонометрия 

Тригонометрия.
Все функции для тригонометрии должны находиться в файле src/lib/services/math/trigon.js

функции, которые необходимо создать в файле src/lib/services/math/trigon.js
		sinFunc,
		sinhFunc,
		asinFunc,
		asinhFunc,
		cosFunc,
		coshFunc,
		acosFunc,
		acoshFunc,
		tanFunc,
		tanhFunc,
		atanFunc,
		atanhFunc,
		cotFunc,
		cothFunc,
		acotFunc,
		acothFunc,
    cornerSwitch


стандартный Math.sin() принимает радианы, а пользователи калькуляторов обычно ждут градусы. Поэтому стор в файле appState.svelte.js указано corner, и есть функция cornerSwitch  которая переключает режим обработки тригонометрии. 

class AppState {
 corner = $state('deg'); // Флаг в каких еденицах измерения работаем с тригонометрией. По умолчанию градусы - 'deg', возможные варианты 'deg', 'rad' - радианы, 'grad' - грады.  
}
Продумай наличие функции конвертации: rad = deg (pi/180)

Переключение для аppState.corner осуществляется кнопками с классом btn__trig, та кнопка текст в которой равен тексту в аppState.corner - имеет дополнительный класс active. 

Компонент с кнопками для тригонометрических функций в файле src/lib/components/Btn/BtnBlockFeatures/BtnBlockOpTrigon.svelte
<script>
	/**
* BtnBlockOpTrigon.svelte

*/
	import BtnText from '../BtnText.svelte';
	import {
		sinFunc,
		sinhFunc,
		asinFunc,
		asinhFunc,
		cosFunc,
		coshFunc,
		acosFunc,
		acoshFunc,
		tanFunc,
		tanhFunc,
		atanFunc,
		atanhFunc,
		cotFunc,
		cothFunc,
		acotFunc,
		acothFunc
	} from '$lib/services/math/trigon.js';
</script>

<div class="digBlock">
	<div class="row">
		<BtnText customClass="op btn__trig active" onclick={() => trigCheck()} buttonText="deg" />
		<BtnText customClass="op btn__trig" onclick={() => trigCheck()} buttonText="rad" />
		<BtnText customClass="op btn__trig" onclick={() => trigCheck()} buttonText="grad" />
	</div>
	<div class="row">
		<BtnText customClass="op btn__func" onclick={() => sinFunc()} buttonText="sin" />
		<BtnText customClass="op btn__func" onclick={() => sinhFunc()} buttonText="sinh" />
		<BtnText customClass="op btn__func" onclick={() => asinFunc()} buttonText="asin" />
		<BtnText customClass="op btn__func" onclick={() => asinhFunc()} buttonText="asinh" />
	</div>
	<div class="row">
		<BtnText customClass="op btn__func" onclick={() => cosFunc()} buttonText="cos" />
		<BtnText customClass="op btn__func" onclick={() => coshFunc()} buttonText="cosh" />
		<BtnText customClass="op btn__func" onclick={() => acosFunc()} buttonText="acos" />
		<BtnText customClass="op btn__func" onclick={() => acoshFunc()} buttonText="acosh" />
	</div>
	<div class="row">
		<BtnText customClass="op btn__func" onclick={() => tanFunc()} buttonText="tan" />
		<BtnText customClass="op btn__func" onclick={() => tanhFunc()} buttonText="tanh" />
		<BtnText customClass="op btn__func" onclick={() => atanFunc()} buttonText="atan" />
		<BtnText customClass="op btn__func" onclick={() => atanhFunc()} buttonText="atanh" />
	</div>
	<div class="row">
		<BtnText customClass="op btn__func" onclick={() => cotFunc()} buttonText="cot" />
		<BtnText customClass="op btn__func" onclick={() => cothFunc()} buttonText="coth" />
		<BtnText customClass="op btn__func" onclick={() => acotFunc()} buttonText="acot" />
		<BtnText customClass="op btn__func" onclick={() => acothFunc()} buttonText="acoth" />
	</div>
</div>


===================================================================
=================================================================== 

# Конвертер углов (Deg / Rad / Grad): Возможность переключаться между градусами и радианами. Для инженеров радианы — стандарт, для школьников — градусы.



# Обратные тригонометрические функции: Не только sin, но и asin (arcsin), acos, atan.





# BRANCH i18nStart


# сменить название и вместо https://axi0man.github.io/axio_calcul/ должно быть [[[[[ amoca ]]]]] - axio mobile calculator
# SEO наименование страниц в routes вместе с chatGPT
# SEO тексты и инструкции для страниц вместе с chatGPT

# разобраться с i18n. На старте два языка RU EN, а потом добавить ещё.

В экосистеме SvelteKit для работы с i18n обычно используют динамические функции для формирования путей.

# проверить ссылки с учётом перспективы применения i18nStart (src/routes/[[lang]]/(math)/basic/+page.svelte).
<a href="{base}/history">
или
<a href="/history">
При локальном тестировании через npm run serv - работают обе ссылки, после публикации ссылки без {base} - не работают.


# Размер шрифта в зависимости от количества букв
Если текст статический (через bind:textContent)
Если текст прописан прямо в верстке, но вы хотите, чтобы классы реагировали на него (например, при переводах через словари), можно использовать привязку к содержимому.

Svelte
<script>
  let titleEl; // ссылка на элемент
  let text = $state("");
</script>

<h3 class="catalog__card--title">
  <span 
    bind:this={titleEl}
    bind:textContent={text}
    class:long-title={text.length > 9}
    class:extra-long-title={text.length > 15}
  >
    trigonometry
  </span>
</h3>


# проверить шрифты


# Рекламу перевести на английский?




=================================================================== 
===================================================================
===================================================================



# BRANCH text 

# страница install: краткое объяснение что установится как и зачем
1. если это iOS (Safari) - то показать инструкцию как установить Пользователи iPhone могут установить PWA только вручную через кнопку «Поделиться» (Share) -> «На экран "Домой"» (Add to Home Screen). Совет: Для iOS -> обычно показывают текстовую подсказку с картинкой (инструкцию), как именно нажать кнопку «Поделиться».
2. если Android (Chrome) или Desktop (Chrome/Edge) -> (<../../../Link to svelte5doc/assets/CODING/setupPWAbutton.md>)




 
# почему иконки для каталога страниц, первоначально грузятся несколько секунд???






===================================================================
=================================================================== 


# дроби - отдельная страница внутри математики и добавить всё из инженерного 

÷ &divide; дробь
1÷2=1/2 - где 1 сверху, а 2 снизу
(1+3)÷2=4/2


≡ &equiv; тождество
4/2 ≡ 2
7/2 ≡ 3*1/2 

:.  преобразовать обычную дробь в десятичную
 3*1/2 :.  3,5

+ _ / * обычные операции
1/2 + 4/2 -1 = 3/2

3/2 * 4 = 12/8

12/8 / 1/2 = 24/8 ≡ 3


# пропорция - отдельная страница  внутри математики




 

#  ❎ Поскольку ты переходишь со страницы на страницу, помни: твой appState — это синглтон. Если ты ввел число на главной, а потом перешел по ссылке /basic, значение appState.display сохранится, так как объект в памяти один и тот же.
Если ты хочешь, чтобы при переходе в новый режим калькулятор "сбрасывался", тебе стоит вызывать appState.reset() в блоке onMount на целевой странице или прямо в функции клика по ссылке.
appState — это синглтон для всей математики!!









============================================
============================================

# BRANCH sound

Settings Page звуки вкл/откл
Звуки нажатия кнопок, функций, открытия окон и т.п.





# работает, но выводит на экран значение - должно быть так - через таймер 500мсек очищается ячейка и всё
// СЦЕНАРИЙ 1: Долгое нажатие на кнопку памяти -> ОЧИСТКА - НЕ РАБОТАЕТ!!!
  if (isLongPress && slot && !sourceData) {
    appState.memoryUpdate(slot, null);
    return;
  }




============================================
============================================
# 


# 2. Физические константы (для настоящего инженерного уровня)
Символ,Название,Значение,Применение
c,Скорость света,299792458 м/с,"Физика, оптика"
G,Гравитационная постоянная,6.674×10−11,Астрономия
g,Ускорение свободного падения,9.80665 м/с²,"Механика, расчет веса"
h,Постоянная Планка,6.626×10−34,Квантовая физика
R,Газовая постоянная,8.314 Дж/(моль·К),Термодинамика

# 

# Карта сайта в футер или nav

# Убрать все TODO,  все console, все комментарии

# 

# 

# 


# с развитием проекта - появляется много строк с $effect(() => { и onMount(() => { в src/routes/+layout.svelte и src/routes/+page.svelte - это не вредит скорости приложения и не засоряет память устройства? Не стоит обращать на это внимание, так как Svelte5 всё это отслеживает или моё волнение оправдано и необходимо применить определённые действия? 
Инструкция в  ../../svelte5doc/assets/CODING/audit app.docx 

# Ещё вопрос: строки appState.display и appState.expression - как можно их корректировать установив курсор и используя клавиатуру приложения, например backspase цифры и знаки И ОСОБЕННО БЫЛО БЫ УДОБНО - ВСТАВИТЬ ЦИФРЫ из буфера!!! В буфер удобно было бы брать записи со страницы history (не в дисплее калькулятора, а именно с отдельной страницы) ПРОВЕРЯТЬ ЧТО ВСТАВИЛИ допустимые значения будет уже существующий код!!

# чтобы при нажатии клавиши на клавиатуре соответствующая кнопка на экране "подсвечивалась" (визуальный отклик) -  добавить в appState временный флаг нажатой клавиши, но это уже "косметика" для продвинутых юзеров. 😉
